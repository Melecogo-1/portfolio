# 作品集站点 · 部署与使用说明

纯静态站点：HTML + CSS + 原生 JS + Three.js（走本地 `vendor/`），**无后端、不连任何外部接口、不依赖 CDN 与在线字体**，断网可运行。整包约 65MB（含五个项目源码），其中 3D 人物模型约 15MB。

---

## 一、在自己电脑上预览

直接双击 `index.html` 也能打开，但个别浏览器会因本地安全策略拦截 3D 模型，推荐起一个本地静态服务：

1. 在文件夹空白处按住 Shift 点右键 →「在此处打开 PowerShell / 终端」；
2. 执行 `python -m http.server 8080`（没有 Python 时，用 VS Code 的 Live Server 插件亦可）；
3. 浏览器打开 `http://127.0.0.1:8080`。

操作：点环绕人物的档案卡进项目，`←` `→` 切换，`Esc` 关闭；右下角「创意终端」是纯本地的创意生成器；右上角「履历 / CV」是速览简历，可下载投递版 PDF。

---

## 二、上线成别人能打开的 https 链接（不用 Docker、不用买服务器、不用备案）

本地的 `127.0.0.1` 只有自己能访问。这是纯静态站，传到静态托管平台即可，自带 https。

### 主方案：GitHub Pages（本仓库已在 GitHub，最省事）

1. 仓库推送到 GitHub 后，进入仓库 **Settings → Pages**；
2. Source 选 **Deploy from a branch**，Branch 选 `main`、目录选 `/ (root)`，保存；
3. 约一分钟后得到 `https://melecogo-1.github.io/portfolio/`，这就是可投递的作品链接；之后本地 `git push`，页面会自动更新。

### 备选 A：腾讯 EdgeOne Pages（国内手机打开更快，免费额度够用）

EdgeOne 控制台 → **Pages → 创建项目 → 直接上传**，把整个文件夹拖入（或传 zip），稍等片刻得到 `https://xxxx.edgeone.app` 地址。

### 备选 B：Netlify Drop（海外，免登录可临时出链）

打开 `https://app.netlify.com/drop`，把文件夹拖进网页即得 `https://xxx.netlify.app`，最快；国内网络偶尔偏慢。

> 之后改内容：本地改完重新推送 / 重新上传覆盖，链接不变。

---

## 三、求职时具体怎么发

- **BOSS 直聘**：在线简历的「个人 / 作品链接」处填上线地址；HR 索要作品时把链接贴进对话框。
- **HR 让发邮箱**：正文写一句「在线作品集：https://你的地址」，并把 `resume/` 里的简历 PDF 作为附件附上（线上浏览 + 离线留档都给到）。
- **精准直达某个板块**，在链接后加锚点：
  - 首页：`/`
  - 第 N 个项目：`#p0`～`#p4`（0=AI-DataPilot，1=Dream Weaver，2=多服务部署，3=Creative Engine Lab，4=Silent Asylum）
  - 直接弹出简历：`#cv`

  例：`https://你的地址/#p1` 打开就是 Dream Weaver 的项目档案。

---

## 四、之后补「进入在线体验」链接（可选）

子项目目前没有公网地址，按钮显示「本地可运行 · 欢迎现场演示」，不会写死成别人打不开的 `127.0.0.1`。某个子项目以后上线了：打开 `app.js`，在顶部 `projects` 数组里把对应项目的 `launchUrl: null` 改成线上地址，按钮会自动变成「进入在线体验 ↗」并在新标签页打开。

---

## 五、目录结构

```
portfolio/
├─ index.html / styles.css / app.js   3D 作品集站
├─ vendor/                            three.js 与模型加载器（本地，无 CDN）
├─ assets/                            3D 模型、封面、头像与简历 PDF
├─ projects/                          五个后端项目源码，各含 README 与运行步骤
├─ resume/                            校招简历 PDF
└─ README.md                          仓库门面说明
```

新增项目：在 `app.js` 的 `projects` 数组照现有格式加一条，轨道会自动多一张卡；封面图放 `assets/projects/` 并在 `cover` 字段引用，没有封面就把 `useAbstract` 设为 `true`，用程序化色块代替。

---

## 六、兼容性

- 桌面、手机均做了自适应；手机上项目卡收成右侧列表，点开为全屏档案。
- 遵循系统「减弱动态效果」设置；不支持 WebGL 的老设备自动降级为 2D 氛围背景，内容不受影响。
- 全站无外部请求，首屏有加载进度层，不会白屏。
