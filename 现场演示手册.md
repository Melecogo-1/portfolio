# 现场演示手册

> 面试现场照着做即可。每条启动命令都在本机逐条实测过（2026-09）。
> 分两层：**线上层**不依赖本机、断网也能开；**本地层**证明项目真的能跑起来。

---

## 一、开场用线上（先稳住，不依赖现场网络环境）

| 看什么 | 地址 / 入口 | 说明 |
| --- | --- | --- |
| 3D 作品集主站 | https://melecogo-1.github.io/portfolio/ | 纯手写单页，Three.js 走本地 vendor，**零 CDN、断网可开** |
| 创意终端 | 主站右下角「AI · 创意终端」 | 纯前端本地推演一条灵感，不请求外部接口，断网可生成 |
| 01 完整分析案例 | 主站点 01 卡片 →「进入在线体验 ↗」 | 静态案例页，无需后端，是 01 最完整的对外展示 |

---

## 二、本地起服务（证明能落地，命令均实测 HTTP 200）

一次性环境（每个项目各建一次虚拟环境即可）：

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows PowerShell
pip install <对应依赖，见下>
```

### 01 · AI-DataPilot 经营数据分析 —— 端口 5000（王牌，先演示它）

```bash
cd projects\01-ai-datapilot\backend
pip install -r ..\requirements.txt      # Flask flask-cors pandas openpyxl requests
python app.py                           # 打开 http://localhost:5000
```

- **无 DeepSeek Key**：自动进规则模拟模式，看板与图表不受影响，自然语言查数走关键词规则。
- **30 秒路径**：顶部 20.15 亿 / 28.4 万台 → 月度双轴趋势 → 品牌 CR3=88% → 平台图定位快手毛利 -1.70% → 异常猎手 → 自然语言查数。
- **数据口径（被问到时这样答）**：顶部营收 / 销量来自真实聚合表；仓内订单明细是 2800 行**脱敏采样**（企业明细不入公开仓，合规要求），用 `tools/generate_sample_orders.py` 固定种子可复现；全量 4.9 万报价单 / 15.6 万账单口径见案例页与 `data/DATA_NOTE.md`。

### 02 · Dream Weaver 互动叙事后端 —— 端口 5002

```bash
cd projects\02-dream-weaver
pip install Flask requests              # gunicorn 只在线上 Linux 用，Windows 本地跳过
python app.py 5002                      # 打开 http://localhost:5002
```

- **无 Key**：规则裁决照常工作，叙述回退到内置模板，两条降级路径相互独立。
- **讲点**：自由文本输入 → 规则先判 7 类动作 / 可行性 / 风险 → 模型只写叙述、不决定剧情；9 谓词声明式 DSL、态度增量钳 [-8,8]、叙述生成移出状态锁临界区。

### 03 · 一源多服务 + 内容溯源 —— 端口 8010

```bash
cd projects\03-multi-service-deploy\provenance-lab
pip install "Pillow>=10"
python backend\server.py                # 打开 http://localhost:8010
```

- **讲点**：上传图 → SHA-256 去重、8×8 感知哈希 + 汉明距离算相似、parent_id 谱系三校验；根目录 `render.yaml` 一份蓝图编排 4 个服务，环境变量做服务发现，空库幂等自举。

### 04 · Creative Engine Lab —— 端口 8000（免安装）

```bash
cd projects\04-creative-engine-lab
python backend\server.py                # 纯标准库，无需 pip install；http://localhost:8000
```

- **讲点**：不引 Web 框架，手写 HTTP 路由 / MIME / CORS / 目录穿越防护；输入派生随机种子，同输入同结果、可复现；主站里还有它的纯前端复刻，断网可推演同一套逻辑。

### 05 · Silent Asylum 文字游戏 —— 端口 8080

```bash
cd projects\05-silent-asylum
pip install Flask                       # gunicorn 只在线上用
python app.py                           # 打开 http://localhost:8080
```

- **必须经 Flask 打开 `localhost:8080`**：`templates/index.html` 是 Jinja 服务端模板，直接双击会看到未渲染的模板标记，那不是 bug。
- **讲点**：71KB JSON 声明式驱动；侵蚀度单次增量钳 [-20,+30]；选择 / 进入 / 读档三处一致性校验；5 存档槽过场自动存、死亡后原地重试 / 回档 / 软重置。

---

## 三、推荐节奏（约 5 分钟）

1. **30s**　线上 3D 主站 + 创意终端：先证明前端、审美与离线能力。
2. **2min**　01 案例页 / 本地看板：真实数据、口径甄别、逐级下钻定位亏损，是最重的一张牌。
3. **1.5min** 02 或 05 任选其一：证明后端复杂度（规则引擎 / 状态流转与一致性）。
4. **30s**　03 带过部署：一份 Blueprint 编 4 服务、已在 Render 跑通，证明能上线。

---

## 四、应急

- **端口被占**：02 直接 `python app.py 5003` 换端口；其余用 `PORT=端口` 环境变量或改启动端口。
- **现场断网**：主站与 04 创意终端本就离线可跑；01 / 02 / 05 无 Key 自动降级，主功能不受影响。
- **被问数据真假**：01 顶部是真实聚合、订单为合规脱敏采样，`DATA_NOTE.md` 写了复现方式，全量数字与案例页一致——这是数据合规意识，不是藏数据。
- **05 打开是花括号**：说明点到了 Jinja 模板，回到 `python app.py` 走 Flask 渲染。
- **关服务**：启动窗口 `Ctrl+C`，或任务管理器结束对应 python 进程。
